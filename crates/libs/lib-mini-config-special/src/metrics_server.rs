pub fn get_metrics_server(tmp: String) -> mini_config_core::Result<bool> {
    // TODO - how to get a metrics server configuration?  Metrics server should be moved into its own library too?
    let enabled = true;

    Ok(enabled)
}
